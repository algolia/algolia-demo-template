// Product detail page removed for GenCat content demo
// Pages redirect to external GenCat URLs via app/products/[id]/page.tsx
"use client";

export default function ProductPage({ product }: { product: any }) {
  return null;
}
